// Invite-code join screen. POSTs to /api/church-group/join (Sprint 1 #16).
export default async function JoinPage({ params: _params }: { params: Promise<{ code: string }> }) {
  return <h1>Join church group — coming soon</h1>;
}
